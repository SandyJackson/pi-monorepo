---
status: accepted
---

# Version the bash policy with the workspace

`bash-permission.json` is part of the Pi workspace and is shipped with the bash-permission extension. It is a security policy rather than machine-local state, so bootstrap and development installations use the same reviewed rules; policy changes should be treated as security-sensitive changes.
